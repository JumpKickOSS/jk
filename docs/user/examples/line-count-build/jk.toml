group = "demo"
name = "line-count-build"
version = "0.0.1"
java = 25

[application]
main = "demo.App"

# Optional — omit to use the .jk/ convention directory when present.
# [build]
# logic = "tools/codegen"          # only if not using .jk/
