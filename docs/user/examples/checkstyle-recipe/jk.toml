group = "com.example"
name = "checkstyle-recipe"
version = "0.0.1"
java = 25

[application]
main = "demo.Sample"

# Compile-time only (@NullMarked): on javac's path, in no jar.
[provided-dependencies]
jspecify = "1.0.1"

[test-dependencies]
junit-jupiter = "6.1.3"
