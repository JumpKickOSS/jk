# size-baseline.txt — the ratchet behind `checkFileSizeCaps` (JK-2411).
#
# A shipped number is a floor, not a fact. `EngineServer` hit 1,064 lines at the end of its peel and
# was back to 1,240 eleven days later; the `JkManager` triad went 2,467 -> 3,414 in ten days and is
# now larger than the 2,204-line god class it replaced. A cap that is not re-checked is not a cap.
#
# The caps (docs/contributors/code-as-art.md, "House rules -> Size") are per language:
#
#     Java / Kotlin   soft   400   hard   800   exception 1,200, with the invariant comment
#     JavaScript      soft   600   hard 1,200   exception 1,600, with the invariant comment
#     CSS             exempt. Splitting a cascade on line count is a regression risk with no
#                     readability win, so clients/web/.../style.css (2,401 lines) is deliberately
#                     absent from this file and from the guard.
#
# The three rules `checkFileSizeCaps` enforces against this list:
#
#   1. A listed file may only shrink. Its line count must stay <= the number below. Growing it
#      fails the build — that is the whole point of the file.
#   2. A file that is NOT listed must be <= the hard cap for its language. A new 900-line Java
#      file fails. Adding it here instead is the escape hatch, and it is a reviewable diff.
#   3. Every entry carries a comment. Every file here is at or above its hard cap, so every file
#      here is claiming the exception band, and the exception band costs a stated invariant.
#      An entry with no comment above it fails the build. Re-baselining a number upward means
#      deleting the invariant that justified the old one and writing one that justifies the new
#      one — in the same diff, right here.
#
# The count is `wc -l`: newline characters, the same number the scoreboard and commit messages
# cite. Recount one file with `wc -l <path>`.
#
# UPDATING AFTER A SHRINK. Shrinking a listed file passes — the ratchet never blocks progress. The
# guard then prints the tightened line, e.g.
#
#     size-baseline.txt is loose (these files shrank — tighten it in this commit):
#        958  server/engine/src/main/java/cc/jumpkick/engine/EngineServer.java   (was 1223)
#
# Paste that number over the old one, keep or rewrite the comment, and it lands in the same commit
# as the shrink. Deleting a listed file works the same way: drop its entry.
#
# JK-2412 moved 17 of the numbers below UPWARD, by 2 to 16 lines, and 7 downward. Nothing was
# added to any of them: the tree-wide FQCN sweep replaced an inline `cc.jumpkick.foo.Bar` with an
# `import` line, so a file that named a package-qualified type in ten places gained one line and
# lost ten qualifiers. Characters fell everywhere; lines moved either way depending on how much
# re-wrapping the shorter names bought back. The invariants below are unchanged.
#
# JK-2414 moved four of them upward, by 1 to 7 lines, for the same kind of reason: the TaskNames
# sweep replaced `"compile-java"` with `TaskNames.COMPILE_JAVA`, which is eight characters longer,
# so a `case` arm or a constructor call that used to fit on one line now wraps. Two of the four are
# the added `import` line alone. The invariants below are unchanged.
#
# FORMAT. One entry per line: `<lines>  <path from the repo root>`. The comment lines immediately
# above an entry, with no blank line between, are that entry's invariant. A blank line separates
# entries. Sorted by path.

# T56: the wire-event decode order IS the contract — a `job-start` decoded after its first
# `step-start` renders a module that never started. T56 deletes this file outright into
# EngineJobs / EngineReads / EngineEventDecoder / EngineExplainDecoder; until then the ordering
# lives in one switch.
1327  clients/cli/src/main/java/cc/jumpkick/cli/engine/EngineBuildListenerAdapter.java

# T56: request framing and the single socket writer stay together (JK-2385 made that one owner);
# a split hands the writer to a second object and the synchronization with it.
1010  clients/cli/src/main/java/cc/jumpkick/cli/engine/EngineClient.java

# Mode, artifact and `awaitStartup` stay together so a torn AOT cache cannot be mapped without the
# self-heal retry that repairs it — the file's own javadoc names this and the AOT key/manifest
# comments are most of the excess.
864  clients/cli/src/main/java/cc/jumpkick/cli/engine/EngineSpawn.java

# One painter owns the whole failure block: the OSC-8 deep link, the gutter, the source snippet and
# the box drawing are aligned against each other column by column, and a split desynchronises them.
1002  clients/cli/src/main/java/cc/jumpkick/cli/run/TestFailureHighlight.java

# T54 — THE REGRESSION THIS FILE EXISTS TO STOP. The triad (this + JkManagerView 1191 +
# JkManagerColor 678) is 3,414 lines against the 2,204-line class it replaced. The live region owns
# animation state and the Ctrl-C handoff, which is why paint (JkManagerView) and token colour
# (JkManagerColor) are already out and this facade is not further splittable at the seam.
1548  clients/cli/src/main/java/cc/jumpkick/cli/tui/JkManager.java

# T54: paint and settle for the plan-mode frame. 47 dead static delegates go first; what is left is
# one frame budget, and two files cannot share a frame budget.
1193  clients/cli/src/main/java/cc/jumpkick/cli/tui/JkManagerView.java

# Incremental redraw of the active step only, with no alt-screen: the step ladder, the redraw
# cursor arithmetic and the cancel path that restores cursor/SGR are one state machine. Split it
# and a cancel mid-redraw leaves the shell prompt in inverse video.
839  clients/cli/src/main/java/cc/jumpkick/cli/tui/Wizard.java

# T57: the lock -> sync -> compile -> test -> package order is the command's contract, and every
# early-exit arm has to leave the workspace in a state the next verb can resume from. T57 peels
# ProjectPeek / BuildTails / WorkspaceRunView / BuildSelection off the edges, not the order.
1206  clients/cli/src/main/java/cc/jumpkick/command/BuildCommand.java

# T57: 91 forwarders and CURATED_IDS are deleted, and the wizard chrome peels to NewChrome /
# NewPlans. The template-apply step and its rollback must stay in one place: a half-applied
# giter8 template is a directory the user has to clean up by hand.
1120  clients/cli/src/main/java/cc/jumpkick/command/NewCommand.java

# T60: one root component and two views, and `clients/web` has no bundler — a split costs a
# <script> tag and a load-order invariant no compiler checks, which is exactly why the JS band is
# wider than Java's. Over even that band; T60 is the ticket.
2786  clients/web/src/main/resources/web/app.js

# T60: the Monaco AMD loader is SRI-pinned in exactly one place and then fetches editor.main.js,
# the CSS and the per-language chunks itself with no integrity plumbing to pass down. The pin and
# the loader configuration cannot be in different files.
1966  clients/web/src/main/resources/web/code.js

# T60: pure folds of (cards, event) with no browser globals — that purity is what makes the
# activity feed testable headlessly under `node --test`, and the per-card bounds (MAX_CARDS,
# MAX_OUTPUT_LINES, MAX_DIAGNOSTICS) only hold if one fold applies all of them.
1736  clients/web/src/main/resources/web/fold.js

# T59: the four positional overloads become one JavaCompileJob record. The incremental analysis
# store and the compile invocation that writes it must agree on the key, so they stay together.
1195  plugins/java-compiler/src/main/java/cc/jumpkick/java/compiler/ZincJavaCompiler.java

# `lifecycleLock` covers election, accept, drain and close as one lifecycle (JK-1470), and
# `tryStartBuildPlan` IS that invariant — the proposal to peel it was withdrawn on purpose, because
# any extraction hands the lock to a second object and spreads the invariant across two files.
# T53 peels EngineElection + DrainReporter only, which do not touch the lock.
1231  server/engine/src/main/java/cc/jumpkick/engine/EngineServer.java

# The Host-header check, the admission semaphore and the bearer-token gate are one admission
# decision made in one order. Splitting them splits a security control, and a request that passes
# two of three is a request that got in.
839  server/engine/src/main/java/cc/jumpkick/engine/http/HttpEngineServer.java

# T55 — the largest file in the repo, and the least defensible. 63 tool names typed exactly twice,
# five telescoping constructors and a 361-line `toolsList`. The only real invariant is that
# `toolsList` / `toolsCall` / INSTRUCTIONS / PROMPTS agree on every name; T55 replaces that
# coincidence with an McpTool registry and takes the file to about 150 lines.
1673  server/engine/src/main/java/cc/jumpkick/engine/http/McpHandler.java

# T58: the append path and the replay path must read and write the same record shape, and a
# journal that replays differently from how it was written is a corrupted history.
858  server/engine/src/main/java/cc/jumpkick/engine/journal/BuildJournal.java

# T58: `coreBuilder` and `appendDeclaredTails` have to agree on DAG edge order — a tail appended
# against a stale edge set is a step that runs before its input exists.
1255  server/engine/src/main/java/cc/jumpkick/runtime/BuildPlanner.java

# T58: the probe bootstrap, the continuous learned-rate fold and the cold-ETA model are one model.
# Micro-probes must never become absolute method costs (empty @Tests run in ~5 ms and would
# under-shoot real suites by 10x), and that rule is only enforceable where both halves are visible.
1389  server/engine/src/main/java/cc/jumpkick/runtime/Calibration.java

# T58: the per-phase weight table is data that belongs in a resource, not code. No invariant holds
# it here — this entry is a ratchet, not a justification.
1205  server/engine/src/main/java/cc/jumpkick/runtime/EffortWeights.java

# T58: one plan per verb, with the shared step bodies next to the plans that compose them.
1089  server/engine/src/main/java/cc/jumpkick/runtime/ExecPlans.java

# The Jib path and the docker/podman Dockerfile path must produce the same structured success keys
# (IMAGE_REF / TARBALL_PATH / ...) — two files means two key sets and a caller that can only handle
# one of them.
808  server/engine/src/main/java/cc/jumpkick/runtime/ImagePlans.java

# T58: step bodies shared by the Planner* collaborators. No invariant holds it here — ratchet only.
949  server/engine/src/main/java/cc/jumpkick/runtime/PlannerSupport.java

# T58: plugin discovery, the contribution fold and the plugin step bodies. Ratchet only.
826  server/engine/src/main/java/cc/jumpkick/runtime/PluginBuild.java

# T58: the forecast's cache keys must mirror the real build's request field for field — drift makes
# `jk explain` forecast a phantom rebuild. Dirty-marking of a module and of its consumers is one
# traversal, so misses stay pessimistic and never become false cache hits.
1219  server/engine/src/main/java/cc/jumpkick/runtime/TaskForecaster.java

# T58: workspace step execution and the target/ write-drain that `jk build` must not return ahead
# of (JK-2451). Ratchet only.
908  server/engine/src/main/java/cc/jumpkick/runtime/WorkspaceExecute.java

# T58: the `##JKT:` fork protocol has exactly one writer and one reader, and they are the two ends
# of the same wire format. The one-shot and pull-queue worker modes share it.
1373  server/engine/src/main/java/cc/jumpkick/test/JUnitLauncher.java

# T59: the three scope solves (main / test / processor) must stay independent of each other and
# produce one lockfile — a shared mutable universe between them is how a test-only version leaks
# into the main classpath.
1216  server/resolver/src/main/java/cc/jumpkick/resolver/LockOrchestrator.java

# The per-solve version/dependency cache, the transitive prefetch and the preferred-version seeding
# are one PubGrub PackageSource contract; the solver's determinism depends on all three answering
# from the same snapshot.
815  server/resolver/src/main/java/cc/jumpkick/resolver/MavenPackageSource.java

# T59: revisit-marking is what stops a diamond graph exploding, and it is only correct if the walk
# that marks and the render that reads the mark are the same traversal. T59 splits the ANSI styling
# off, not the walk.
1421  shared/core/src/main/java/cc/jumpkick/resolver/DependencyTree.java

# T59: the parsed shape of jk.toml, one nested record per block. It splits cleanly by block and
# there is no cross-block invariant — ratchet only.
1218  shared/jk-api/src/main/java/cc/jumpkick/model/JkBuild.java

# T51 / T59: every wire key is typed twice, once to write and once to decode, and the pair must
# agree. The fix is generation, not a split — two hand-written halves in two files is strictly
# worse than two in one.
1029  shared/wire/src/main/java/cc/jumpkick/engine/protocol/ProtoEvents.java

# T51 / T59: same pairing as ProtoEvents, for the job verbs.
1020  shared/wire/src/main/java/cc/jumpkick/engine/protocol/ProtoJobs.java
