// SPDX-License-Identifier: Apache-2.0
/**
 * IDE project files from the engine's IDE model: the IntelliJ IDEA and VS Code generators, the
 * BSP connection file, and the source-root view they share. Pure model-to-files — nothing here
 * prints, and nothing here talks to the engine — so the CLI and the engine run the same code.
 */
@NullMarked
package cc.jumpkick.ide;

import org.jspecify.annotations.NullMarked;
