name = "jk-plugin-sdk"
description = "Plugin SPI and host-event wire codec"
# Workers load on user JVMs (JDK 17+); stay on release 17.
java = 17

[test-dependencies]
assertj-core = "latest"
junit-jupiter = "latest"
