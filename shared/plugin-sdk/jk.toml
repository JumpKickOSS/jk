name = "jk-plugin-sdk"
description = "Plugin SPI; JSONL codec is jk-jsonl"
# Workers load on user JVMs (JDK 17+); stay on release 17.
java = 17

[dependencies]
jk-jsonl.workspace = true

[test-dependencies]
assertj-core = "latest"
junit-jupiter = "latest"
