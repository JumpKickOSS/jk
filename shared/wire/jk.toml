[project]
name     = "jk-engine-api"
description = "jk engine API: the client<->engine wire contract — protocol codec, paths, build DTOs."

[dependencies]
jk-model.workspace = true
jk-core.workspace = true
jk-plugin-sdk.workspace = true

[test-dependencies]
assertj-core            = "3.27.7"
junit-jupiter           = "6.1.0"
junit-platform-launcher = "6.1.0"
