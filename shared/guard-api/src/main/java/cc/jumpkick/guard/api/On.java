// SPDX-License-Identifier: Apache-2.0
package cc.jumpkick.guard.api;

/** Which elements {@link Facts#annotations(On)} lists. */
public enum On {
    PACKAGE,
    CLASS,
    TEST_CLASS,
    METHOD,
    FIELD,
    PARAMETER
}
