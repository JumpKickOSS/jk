[project]
name     = "jk-toolchain-jdk"
description = "jk toolchain (client slice): JDK flow, discovery probes, launcher shims, exporters."

[dependencies]
jk-core.workspace = true
jk-client-io.workspace = true

[test-dependencies]
assertj-core            = "3.27.7"
junit-jupiter           = "6.1.0"
junit-platform-launcher = "6.1.0"
