[project]
name = "jk-client-io"
description = "Client-side I/O: HTTP, forge auth, credentials, and CAS"

[dependencies]
jk-core.workspace = true

[test-dependencies]
assertj-core = "latest"
junit-jupiter = "latest"
