[project]
name     = "jk-client-io"
description = "jk client I/O: plain-JDK HTTP, forge auth, credential files, CAS read/link surface."

[dependencies]
jk-core.workspace = true

[test-dependencies]
assertj-core            = "3.27.7"
junit-jupiter           = "6.1.0"
junit-platform-launcher = "6.1.0"
