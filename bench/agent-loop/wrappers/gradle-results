#!/usr/bin/env python3
"""Run the pinned Gradle and write `target/jk-results.md` in jk's shape. Arguments are Gradle's; default task `test`."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
import toolrun

sys.exit(toolrun.main("gradle", sys.argv[1:]))
