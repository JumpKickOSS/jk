# The IntelliJ client's own version catalog: this build stands alone and shares no definition
# with the product tree, which jk builds. Error Prone and NullAway move together.
[versions]
errorprone        = "2.50.0"
errorprone-plugin = "5.1.0"
nullaway          = "0.14.1"
nullaway-plugin   = "3.1.0"
jspecify          = "1.0.1"
junit4            = "4.13.2"

[libraries]
errorprone-core = { module = "com.google.errorprone:error_prone_core", version.ref = "errorprone" }
nullaway        = { module = "com.uber.nullaway:nullaway",              version.ref = "nullaway" }
jspecify        = { module = "org.jspecify:jspecify",                   version.ref = "jspecify" }
junit4          = { module = "junit:junit",                             version.ref = "junit4" }

[plugins]
errorprone = { id = "net.ltgt.errorprone", version.ref = "errorprone-plugin" }
nullaway   = { id = "net.ltgt.nullaway",   version.ref = "nullaway-plugin" }
