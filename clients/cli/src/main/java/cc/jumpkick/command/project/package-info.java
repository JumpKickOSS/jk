// SPDX-License-Identifier: Apache-2.0
/**
 * The verbs that edit or explain a project's manifest and lock: new, init, add, remove, sync, lock, tree,
 * why, explain, audit, outdated, update, verify-build, deny, and the new-project wizard's pieces. They
 * read the pipeline family (explain forecasts a build) and the root helpers.
 */
@NullMarked
package cc.jumpkick.command.project;

import org.jspecify.annotations.NullMarked;
