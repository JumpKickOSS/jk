// SPDX-License-Identifier: Apache-2.0
/**
 * The verbs that manage JDKs, tools and the user's shell: jdk, tool, activate, deactivate, shell, trust,
 * wrapper, the completion scripts and the per-shell renderers. They read the pipeline family (tool install
 * and tool run reuse install and run) and the root helpers.
 */
@NullMarked
package cc.jumpkick.command.toolchain;

import org.jspecify.annotations.NullMarked;
