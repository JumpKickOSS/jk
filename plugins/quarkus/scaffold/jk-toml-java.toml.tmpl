[quarkus]
version = "3.28.5"

[dependencies]
quarkus-rest = { group = "io.quarkus", name = "quarkus-rest" }
quarkus-arc  = { group = "io.quarkus", name = "quarkus-arc" }

# quarkus-junit5 pulls maven-embedder; keep off [dev-dependencies] (MAIN graph).
# Add under [test-dependencies] when JK-1202 junit5 thrash is fully closed.
