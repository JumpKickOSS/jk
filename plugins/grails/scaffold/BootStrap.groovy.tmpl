package ${package}

class BootStrap {

    def init = { servletContext ->
        if (Note.count() == 0) {
            new Note(title: 'hello', body: 'from jk').save(flush: true)
        }
    }

    def destroy = {
    }
}
