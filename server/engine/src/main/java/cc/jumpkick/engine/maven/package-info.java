// SPDX-License-Identifier: Apache-2.0
/**
 * What a finished {@code jk mvn} run left behind — the spy's event JSONL, surefire and failsafe
 * XML, the compiler plugin's failure text — read back into the shapes the journal writes.
 */
@NullMarked
package cc.jumpkick.engine.maven;

import org.jspecify.annotations.NullMarked;
