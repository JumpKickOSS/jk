// SPDX-License-Identifier: Apache-2.0
/**
 * The engine process: its entry point, the socket and HTTP fronts it starts, the verb bridge the
 * job envelope dispatches through, and the daemon lifecycle around them.
 */
@NullMarked
package cc.jumpkick.engine;

import org.jspecify.annotations.NullMarked;
