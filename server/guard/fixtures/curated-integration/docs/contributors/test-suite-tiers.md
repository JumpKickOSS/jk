# Test tiers

The branch gate runs the classes `curated-integration.txt` names through
`scripts/curated-integration.sh`, within a budget of 8 minutes of wall clock; the full
integration profile is nightly.
