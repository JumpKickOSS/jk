// SPDX-License-Identifier: Apache-2.0
/**
 * The closed rule vocabulary: every {@link cc.jumpkick.guard.schema.Kind}, its keys and one example
 * each. This table is the single source for load-time validation, {@code jk guard explain --schema}
 * and the manual's kind reference — there is no second copy to drift.
 */
@NullMarked
package cc.jumpkick.guard.schema;

import org.jspecify.annotations.NullMarked;
