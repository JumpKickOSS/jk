// SPDX-License-Identifier: Apache-2.0
/**
 * {@code jk guard explain}: the read-only view of the rule file, the baseline and the last lane
 * outcomes ({@link cc.jumpkick.guard.explain.RuleSummaries}, written by every lane that evaluates),
 * rendered as a rule card, the catalog, or a kind's schema with one example. Nothing here plans or
 * runs a build.
 */
@NullMarked
package cc.jumpkick.guard.explain;

import org.jspecify.annotations.NullMarked;
