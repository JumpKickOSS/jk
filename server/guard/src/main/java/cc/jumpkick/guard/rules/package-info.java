// SPDX-License-Identifier: Apache-2.0
/**
 * Loading {@code jk-guards.toml}: the typed {@link cc.jumpkick.guard.rules.Rule}, the collected
 * {@link cc.jumpkick.guard.rules.LoadError}s (a load never throws past the first mistake), and the
 * one plan-time {@link cc.jumpkick.guard.rules.GuardsPresence} decision that keeps a project without
 * guards from paying for them.
 */
@NullMarked
package cc.jumpkick.guard.rules;

import org.jspecify.annotations.NullMarked;
